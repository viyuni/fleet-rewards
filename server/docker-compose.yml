services:
  guard-rewards:
    image: postgres:18-alpine
    container_name: guard-rewards
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 979979
      POSTGRES_DB: guard-rewards
    ports:
      - '5433:5432'
    # volumes:
    #   - /home/byte/viyuni-database/guard-rewards:/var/lib/postgresql

  guard-rewards-test:
    image: postgres:18-alpine
    container_name: guard-rewards-test
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 979979
      POSTGRES_DB: guard-rewards-test
    ports:
      - '5434:5432'
    # volumes:
    #   - /home/byte/viyuni-database/guard-rewards-test:/var/lib/postgresql
